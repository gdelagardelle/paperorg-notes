# Xcode
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
DerivedData/
build/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3

# Secrets
.env
*.apikey
secrets/

# macOS
.DS_Store

# Exports and recordings (local dev)
Exports/
Recordings/

# Benchmark output
benchmark_results.csv

# Backend
__pycache__/
*.py[cod]
.duo-build/last-dispatch.json

# The backend moved to gdelagardelle/paperorg-notes-api and the vendored
# copy is gone from git, but existing checkouts still have the untracked
# leftovers it accumulated: a 58 MB .venv, a dev SQLite database, a pytest
# cache. Ignore the whole path so they cannot be staged by accident.
/backend/
